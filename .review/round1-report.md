## 结论

修完下列回滚兼容、探测副作用、并发与取消、发版脚本问题再发；方案方向基本合理，但当前实现尚不能保证其声明的隔离与恢复行为。

## 必须修（发版前）

- **凭据新增字段破坏旧版本回滚。** [credential/types.rs:302](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/src/credential/types.rs:302) —— 开启自动续期后，凭据写入 `turn_state_auto_hunt`；基线 OAuth 结构使用 `deny_unknown_fields`，不认识该字段 —— 切回 controls-20260919 后，这些凭据解码失败，账号不能正常使用。建议准备明确的数据降级步骤或兼容回滚镜像，不能只执行容器切换。基线证据：`git show 73250d0:backend/crates/providers/openai/src/credential/types.rs`，第 284–306 行；解码入口见 [security.rs:227](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/src/credential/security.rs:227)。

- **临时出口探测仍会污染真实账号状态。** [provider/mod.rs:601](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/src/provider/mod.rs:601) —— 启用账号的诊断租约仍允许 `account_state_mutation`，判断实际为 `!diagnostic || account.enabled()`（[selector.rs:597](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/src/credential/selector.rs:597)）—— 临时出口的失败仍可写冷却、清 Cookie、影响账号反馈分；多个出口的空正文 404 还能累计 `CloudflarePathBlocked`，达到阈值后将账号置为 Invalid。Core 跳过熔断和探测失败记录无法阻止这些副作用。建议为带 `DiagnosticEgress` 的探测明确隔离出口风险、反馈分及账号失败写入，单独规定真实额度事实如何处理。路径见 [failure.rs:351](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/src/provider/failure.rs:351)、[selector.rs:993](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/src/credential/selector.rs:993)、[diagnostics.rs:505](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/src/transport/diagnostics.rs:505)。

- **旧请求完成时能重新覆盖账号级 pin 的实际优先级。** [turn_state_pin.rs:272](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/src/turn_state_pin.rs:272) —— 普通请求开始时没有 pin；随后 hunt 写入账号级 pin；该普通请求最后完成 —— `completed()` 只检查请求开始时的 `self.value`，仍插入客户端 pin，而后续查找优先客户端 pin，导致旧出口候选重新生效。建议在插入锁内检查当前账号级 pin，并阻止早于显式替换的候选写回。交错依据：[attempt:115](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/src/turn_state_pin.rs:115)、[pin_account_wide:183](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/src/turn_state_pin.rs:183)、[completed:257](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/src/turn_state_pin.rs:257)。

- **命中后没有保证“绑定的就是刚探测的出口”。** [turn_state_hunt.rs:409](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-admin/src/use_case/turn_state_hunt.rs:409) —— `egress.bound` 来自任务开始时的快照；探测期间管理员改绑，仍可能跳过绑定并报告成功。另一条路径是代理 ID 对应地址在探测后变更，`batch_update` 会重新解析最新地址，绑定一个未经本次探测的出口 —— state 与实际出口失配。建议携带探测出口的身份/版本，在提交时原子核对账号绑定及代理地址，变化则中止。依据：[快照生成:176](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-admin/src/use_case/turn_state_hunt.rs:176)、[探测前读取:253](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-admin/src/use_case/turn_state_hunt.rs:253)、[代理提交时解析:224](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-store/src/postgres/proxies.rs:224)。

- **命中前取消仍可能修改账号绑定。** [turn_state_hunt.rs:284](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-admin/src/use_case/turn_state_hunt.rs:284) —— 页面在等待探测响应期间取消，随后该请求返回匹配 state —— 响应后没有再次检查接收端是否关闭，直接进入 `finalize()`。这与页面“账号设置未改动”的提示及 API 的取消承诺冲突。建议在接受命中、进入不可取消收尾前重新检查取消状态，并明确该提交边界。依据：[命中分支:310](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-admin/src/use_case/turn_state_hunt.rs:310)、[取消提示:133](/home/wuxiran/worktrees/cpr-review-hunt-20260920/frontend/src/views/accounts/composables/useAccountTurnStateHunt.ts:133)、[docs/api.md:657](/home/wuxiran/worktrees/cpr-review-hunt-20260920/docs/api.md:657)。

- **账号失败分类使用了实际链路不会产生的错误类别。** [turn_state_hunt.rs:540](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-admin/src/use_case/turn_state_hunt.rs:540) —— 模型不支持实际映射成 `GatewayErrorKind::Unsupported`，认证/权限错误映射成 `UpstreamUnavailable`；这里却匹配 `ModelNotFound / Unauthorized / PolicyDenied`，且所有带 403 的失败优先当成出口问题 —— 已明确不可通过换出口解决的错误仍会遍历其它代理；续期还会落入较短的失败退避。建议从探测结果保留并使用 Provider 的业务分类，仅将明确的出口拒绝视为可换代理。映射证据：[failure.rs:1303](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/src/provider/failure.rs:1303)、[error.rs:1071](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-core/src/error.rs:1071)。

- **续期队列中的账号停用或关闭续期后，仍可能开始真实探测。** [turn_state_renewal.rs:68](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-admin/src/turn_state_renewal.rs:68) —— 整轮先取得待续期列表，再逐个等待完整遍历；排在后面的账号期间被停用、标记凭据失效或关闭自动续期 —— 启动 hunt 不重新核对这些条件，诊断选号又明确绕过停用和可用性限制，因此仍会发请求，甚至换绑。建议后台任务在执行前重新核验最新续期意图及账号状态，并在长遍历的请求边界响应停用。依据：[首次过滤:608](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/src/admin.rs:608)、[prepare:544](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/src/admin.rs:544)、[诊断资格合同:415](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-core/src/account/selection.rs:415)。

- **脚本把检查失败误判成可继续，末尾健康检查也不决定退出状态。** [release.sh:42](/home/wuxiran/worktrees/cpr-review-hunt-20260920/.review/release.sh:42) —— `status` 失败或 JSON 解析失败都会进入“没有旧形态容器，跳过”；`if` 条件中的失败不会触发 `set -e`。末尾 [release.sh:53](/home/wuxiran/worktrees/cpr-review-hunt-20260920/.review/release.sh:53) 也只是打印状态，即使得到 `000/502` 仍可退出成功 —— 错误被掩盖。建议区分查询失败、解析失败和确实不存在，并在宣布完成前断言两个入口均健康。只读复现：提取第 42–46 行、以 `ROLLOUT=false` 执行，输出为 `没有自带别名的旧形态容器，跳过`、`continued-to-deploy`，退出码 **0**。

- **脚本的“可重复执行”和统一回滚说明不成立。** [release.sh:49](/home/wuxiran/worktrees/cpr-review-hunt-20260920/.review/release.sh:49) —— 成功发版且旧槽位排空后重跑，会再次部署同一镜像到另一槽位，删除原来的基线容器，最终两个槽位都是新版本；再执行 `rollback` 无法回到 controls。另在第 2 步删除 legacy、但第 3 步尚未创建新槽位或创建失败时，所声明的 `rollback` 会因另一容器不存在而拒绝。建议增加目标版本已生效的幂等检查，保留独立的基线镜像恢复入口，并写清各失败阶段的恢复命令。语义依据：[rollout.py:213](/home/wuxiran/worktrees/cpr-review-hunt-20260920/.review/rollout.py:213)、[257](/home/wuxiran/worktrees/cpr-review-hunt-20260920/.review/rollout.py:257)、[326](/home/wuxiran/worktrees/cpr-review-hunt-20260920/.review/rollout.py:326)、[343](/home/wuxiran/worktrees/cpr-review-hunt-20260920/.review/rollout.py:343)。

## 建议修（可发版后）

- **清理失去续期资格的退避记录。** [turn_state_renewal.rs:57](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-admin/src/turn_state_renewal.rs:57)：表并非只增不减，成功时会删除；但失败后删除账号、停用或关闭续期，该账号不再进入循环，其记录永久残留。建议按到期时间或有效账号集合清理。

- **为续期增加整轮预算与取消响应。** [turn_state_renewal.rs:98](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-admin/src/turn_state_renewal.rs:98)：串行执行避免单实例同时启动所有账号，但一个慢账号会阻塞后续账号；收集整个事件流时不响应 worker 取消。单次探测 deadline 是十分钟（[execution.rs:46](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-core/src/engine/execution.rs:46)），五分钟提前量不能保证整队列及时续上。建议限制整轮耗时，逐事件消费，并传播取消。

- **不要将当前哈希实现描述为通用 Python 等价序列化。** [repair.rs:73](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/src/credential/revive/repair.rs:73)：递归排序能处理 `preserve_order`，但 `arbitrary_precision` 保留数字表示。锁定依赖为 serde_json 1.0.151，其 [number.rs:282](/home/wuxiran/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/serde_json-1.0.151/src/number.rs:282) 和 [ser.rs:1727](/home/wuxiran/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/serde_json-1.0.151/src/ser.rs:1727) 证明这一点。只读 Python 对照中，`0.000001 → 1e-06`、`1e-7 → 1e-07`，哈希均不相等。后果是漏修，尚无据认定实际导出包含这些字段；建议增加 Python 生成的黄金样本并明确支持范围。

- **续期新鲜度应包含当前长度规则。** [turn_state_pin.rs:209](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/src/turn_state_pin.rs:209)：`account_wide_captured_at` 不匹配 `expected_length`。套餐改变导致规则改变时，实际请求已不能命中旧 pin，续期仍可能把它视为新鲜；建议按完整有效作用域判断。

- **校准请求计数名称。** [turn_state_hunt.rs:337](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-admin/src/use_case/turn_state_hunt.rs:337)：除 busy 外的 `NotSent` 失败也增加 `requests`，因此它表示探测尝试次数，并非界面所称的“已发出请求数”。建议分开统计或调整文案。

- **加强 GET 状态变更接口的来源校验。** [handlers.rs:602](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-api/src/admin/accounts/handlers.rs:602)：`Accept` 可以由脚本设置，不能充当来源证明。现有 Lax Cookie 能限制真正跨站的子资源请求，但不能隔离同站不同源；若存在不可信兄弟子域，该请求仍可能携带 Cookie，CORS 拒绝读取响应也不能撤销副作用。建议使用来源校验或 POST 启动任务；实际部署是否存在该攻击前提见下节。

## 我核实过没问题的点

- **审查范围与只读状态。** `git log --oneline 73250d0..HEAD` 仅列出指定三个提交；结束时 `git rev-parse HEAD` 仍为 `9466049cdce66da7e064d6a858ea32e8dc051df5`，`git status --short` 与开始一致，仅 `?? .review/`。没有执行发版或修改文件。

- **主要敏感类型的 Debug 已脱敏。** `TurnStateHuntTicket.binding` 显式输出 `<redacted>`（[provider.rs:156](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-admin/src/ports/provider.rs:156)）；响应头名称和值均脱敏（[event.rs:514](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-core/src/event.rs:514)）；代理 Debug 不展开 URL，SSE 使用的 `endpoint()` 删除用户名和密码（[proxy.rs:37](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-core/src/account/proxy.rs:37)）。这不等于自由文本错误路径也已证明安全。

- **普通跨站 fetch 不能仅靠 `credentials:'include'` 绕过 Lax。** Cookie 明确设置 `SameSite=Lax`（[session_cookie.rs:20](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-api/src/session_cookie.rs:20)）；跨站子资源请求即使设置 `Accept: text/event-stream`，仍受 SameSite 限制。普通顶层导航可带 Lax Cookie，但不会自行携带该 SSE Accept。这里只确认两层防护的逻辑边界，未进行浏览器实测。

- **pin 的固定 TTL、容量替换与锁范围。** [turn_state_pin.rs:72](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/src/turn_state_pin.rs:72)、[164](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/src/turn_state_pin.rs:164)、[257](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/src/turn_state_pin.rs:257)：命中只增加 hits，不刷新捕获时间；被动候选按请求开始时间计龄；显式替换先删除同作用域旧 pin 再检查容量，所以满表中的同作用域替换不会误拒绝；没有持锁跨越异步请求。上述结论不包含已列出的并发写回问题。

- **HuntGuard 覆盖完整后台任务，常规收尾失败可重试。** [turn_state_hunt.rs:109](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-admin/src/use_case/turn_state_hunt.rs:109)、[139](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-admin/src/use_case/turn_state_hunt.rs:139)、[428](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-admin/src/use_case/turn_state_hunt.rs:428)：账号占位随后台任务退出释放；进入收尾后断开页面不会丢弃该任务；绑定失败不继续 pin，pin 失败保留出口并返回错误。下次遍历优先当前绑定出口。不过 `batch_update` 的配置发布失败可能发生在数据库提交之后（[accounts.rs:671](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-admin/src/use_case/accounts.rs:671)），不能将错误响应理解为绑定一定没变。

- **续期没有单实例全账号并发启动，也没有跨实例租约误用。** [turn_state_renewal.rs:68](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-admin/src/turn_state_renewal.rs:68) 是逐账号串行；Conflict 跳过而不计失败；成功移除退避。注册使用不同 owner、`lease: None`（[lib.rs:485](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-admin/src/lib.rs:485)）。不能由此推断启动总请求量或多实例同时换绑没有风险。

- **出口选择与 WebSocket 池隔离链路正确。** [provider/mod.rs:529](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/src/provider/mod.rs:529) 的克隆账号用于位置和客户端出口，认证、Cookie 和反馈归属仍使用真实 lease；[client.rs:98](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/src/transport/client.rs:98) 将完整代理 URL 纳入出口哈希，[client_sse.rs:490](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/src/transport/client_sse.rs:490) 将其纳入 WS 池 key。即使连接留池，也不会因相同账号而跨不同出口命中。

- **续期配置的常规保留/清除路径正确。** Token 刷新修改完整凭据中的 token 字段，保留续期配置（[repository.rs:53](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/src/credential/repository.rs:53)）；已有账号重导入显式保留配置（[security.rs:244](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/src/credential/security.rs:244)）；关闭 pin 清除续期，重新提交 `pinTurnState:true` 更换 generation，仅提交续期参数则不更换（[credential/admin.rs:716](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/src/credential/admin.rs:716)）。

- **数字修复有精确匹配门槛，嵌套账号分支存在。** [repair.rs:24](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/src/credential/revive/repair.rs:24) 支持顶层 manifest 配合 `data.accounts`；第 58 行仅在候选哈希与记录相等时替换账号；没有命中时返回原字节。至少一个命中后会重新序列化整个文档，因此不保证其余内容的原始排版不变。

- **已执行的验证通过。** `python3 -B -m unittest discover -s tools/revive -p test_revive.py -v`：**8 项通过**，覆盖 summary/result 合并后的复活流程；`bash -n .review/release.sh`、`git diff --check 73250d0 HEAD` 均退出 **0**；仅解析 `.review/rollout.py` AST 成功。均未调用生产服务。

## 没能核实的

- **不能确认所有错误路径均不泄密。** 新增 revive 日志直接输出 `last_error`，`log_text()` 只去控制字符并截断，不脱敏（[client.rs:84](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/src/credential/revive/client.rs:84)、[291](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/src/credential/revive/client.rs:291)）；hunt 的 SSE 错误使用 `client_message()`，可携带上游原始 message（[turn_state_hunt.rs:352](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-admin/src/use_case/turn_state_hunt.rs:352)、[error.rs:1163](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-core/src/error.rs:1163)）。未获得匹配部署版本的上游错误生成源码，不能确认这些字符串永不包含凭据；也没有证据声称已经发生真实泄露。建议收敛为明确允许的错误码与静态消息。

- **代理差异、state 一小时有效性及观澜接口的真实行为。** 这些前提来自简报；本次没有发送真实账号请求。基线与新增测试支持“误读 result 视图计数”的修复方向，但 mock 不能替代上游运行证据，见 [revive 测试:273](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/tests/credential/revive.rs:273)。

- **legacy 退役期间的实际可用性。** [rollout.py:325](/home/wuxiran/worktrees/cpr-review-hunt-20260920/.review/rollout.py:325) 在停止前后做健康检查，但双持别名期间，停止过程中的 DNS 缓存、新连接及既有连接行为未验证。不能确认脚本所称“期间流量全部走 gate”；需从 sub2api 侧验证整个排空窗口，而非只有前后探活。

- **生产产物、回滚镜像和同站来源边界。** 只核对了 [release.sh:25](/home/wuxiran/worktrees/cpr-review-hunt-20260920/.review/release.sh:25) 的校验命令，未访问生产镜像包、metadata、迁移清单、nginx 配置或实际 Docker 状态；也未确认是否存在不可信兄弟子域，不能宣称完整 CSRF 防护已经实测通过。

- **Rust、浏览器和生产集成验证未执行。** 为遵守“不修改任何文件”，没有运行生成构建产物的 Rust/前端命令。现有测试也缺少这些关键覆盖：账号级 pin 与在途被动捕获交错（[pin 测试:109](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/providers/openai/tests/turn_state_pin.rs:109)）；取消后命中、探测后改绑及绑定/pin 故障注入（[hunt 测试:180](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-admin/tests/use_case/turn_state_hunt.rs:180)）；真实 Provider 的诊断副作用隔离——现有 Core 探测测试仍全部传 `egress: None`（[execution 测试:895](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-core/tests/engine/execution.rs:895)）；续期队列中停用、关闭配置和取消（[续期测试:449](/home/wuxiran/worktrees/cpr-review-hunt-20260920/backend/crates/gateway-admin/tests/use_case/turn_state_hunt.rs:449)）；旧版凭据解码，以及数字修复的嵌套、拒绝改写、指数与转义黄金样本。现有 Rust 测试只做源码核查，未计为通过。